# Stories placeholder

Le dossier `stories` accueillera les stories Storybook 8 pour les composants UI (Button, Dialog, DataTable...).
Chaque story devra documenter les variantes accessibles (focus, roles ARIA) et relier les tokens de theme.
