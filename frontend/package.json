{
    "name": "frontend-codex-bootstrap",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "vitest run --reporter=dot"
    },
    "devDependencies": {
        "vitest": "^1.6.0"
    }
}
