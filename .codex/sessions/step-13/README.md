# Step 13 - Release Monitoring Capture

Fichiers:

* `ops-status.txt` - sortie ASCII de `python -m scripts.monitor_release --limit 3`.
* `ops-status.json` - export JSON de `python -m scripts.monitor_release --limit 2 --format json`.
